{% extends 'base.html.twig' %}

{% block title %} {{ title }} {% endblock %}

{% block body %}
<form method="post" class="container col-7 center-element mt-1 p-5">
    {% if error %}
        <div class="alert alert-danger">{{ error.messageKey|trans(error.messageData, 'security') }}</div>
    {% endif %}

    {% if app.user %}
        <div class="mb-1">
            You are logged in as {{ app.user.userIdentifier }}, <a href="{{ path('app_logout') }}">Logout</a>
        </div>
    {% endif %}

    <h1 class="h3 mb-1 font-weight-normal">Veuillez vous authentifier</h1>
    <label for="inputEmail" class="mt-2">Email</label>
    <input type="email" value="{{ last_username }}" name="email" id="inputEmail" class="form-control mt-2 w-75" autocomplete="email" required autofocus>
    <label for="inputPassword" class="mt-4">Password</label>
    <input type="password" name="password" id="inputPassword" class="form-control mt-2 w-75" autocomplete="current-password" required>

    <input type="hidden" name="_csrf_token"
           value="{{ csrf_token('authenticate') }}"
    >

    
        {# Uncomment this section and add a remember_me option below your firewall to activate remember me functionality.
        See https://symfony.com/doc/current/security/remember_me.html #}

        <div class="checkbox p-3">
            <label>
                <input type="checkbox" name="_remember_me"> Remember me
            </label>
        </div>
   

    <button class="btn btn-lg btn-primary" type="submit">
        Se connecter 
    </button><span class="ms-3 fs-6" id="register">Pas de compte? <a href="/register" class="fs-6 text-decoration-none"> Inscrivez-vous.</a></span>
</form>

{% endblock %}
